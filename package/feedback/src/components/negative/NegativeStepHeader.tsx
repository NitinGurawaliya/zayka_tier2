const NegativeStepHeader = () => {
  return (
    <div className="text-center space-y-2">
      <h2 className="text-xl font-semibold text-gray-900">We're listening</h2>
      <p className="text-sm text-gray-500 max-w-xs mx-auto leading-relaxed">
        Please guide us to the exact issue so we can improve quickly.
      </p>
    </div>
  );
};

export default NegativeStepHeader;
